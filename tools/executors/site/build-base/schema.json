{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "cli": "nx",
  "properties": {
    "watch": {
      "type": "boolean",
      "description": "Enable re-building when files change.",
      "default": false
    }
  }
}
