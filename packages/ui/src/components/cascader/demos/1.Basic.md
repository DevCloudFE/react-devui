---
title:
  en-US: Basic
  zh-Hant: 基本
---

# en-US

The simplest usage.

# zh-Hant

最简单的用法。

```tsx
import { DCascader } from '@react-devui/ui';

const list = Array(3)
  .fill(0)
  .map((item, index1) => ({
    label: `Item ${index1}`,
    value: `${index1}`,
    children: Array(5)
      .fill(0)
      .map((item, index2) => ({
        label: `Item ${index1}-${index2}`,
        value: `${index1}-${index2}`,
        disabled: index2 === 3,
        children: Array(20)
          .fill(0)
          .map((item, index3) => ({
            label: `Item ${index1}-${index2}-${index3}`,
            value: `${index1}-${index2}-${index3}`,
          })),
      })),
  }));
export default function Demo() {
  return <DCascader style={{ width: 200 }} dList={list} dPlaceholder="Basic"></DCascader>;
}
```
