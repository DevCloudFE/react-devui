export const LOGIN_PATH = '/login';

export const TITLE_CONFIG: {
  default: string;
  separator: string;
  prefix?: string;
  suffix?: string;
} = {
  default: 'Platform',
  separator: ' - ',
  suffix: 'Platform',
};

export const PREV_ROUTE_KEY = 'from';
